name: fts-rd-agent

services:
  rd-agent:
    image: ghcr.io/medizininformatik-initiative/fts/research-domain-agent:latest@sha256:dca170b3e29f49dfaf444c68da43ddb4bdfb35482faef171b47284d9e6157250
    ports: [ ":8080" ]
    networks: [ "agents", "research-domain" ]                                              # CI_ONLY
    volumes:
    - ./projects:/app/projects
    - ../deidentifhir/:/app/projects/example/deidentifhir                                  # CI_ONLY
    depends_on:                                                                            # CI_ONLY
      rd-hds:                                                                              # CI_ONLY
        condition: service_healthy                                                         # CI_ONLY
    healthcheck:
      test: [ "CMD", "wget", "-qO-", "http://localhost:8080/actuator/health" ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 60s
